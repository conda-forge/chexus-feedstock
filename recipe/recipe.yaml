context:
  name: chexus
  version: "26.6.1"
  python_min: "3.10"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/chexus-${{ version }}.tar.gz
  sha256: b7c1259bb2ad9da9d8888a657b34824b81e37e48b491b1191c67b35c5d097b64

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - chexus = chexus.__main__:main

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=68
    - setuptools-scm >=8.0
    - pip
  run:
    - python >=${{ python_min }}
    - h5py

tests:
  - python:
      imports:
        - chexus
      pip_check: true
      python_version: ${{ python_min }}.*
  - requirements:
      run:
        - python ${{ python_min }}.*
    script:
      - chexus --help

about:
  summary: Validate and check NeXus files
  license: BSD-3-Clause
  license_file: LICENSE
  repository: https://github.com/scipp/chexus
  homepage: https://scipp.github.io/chexus/

extra:
  recipe-maintainers:
    - MridulS
